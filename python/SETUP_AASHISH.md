# Setup

1. `cd python`
2. `python setup.py`
3. Edit `config/api_keys.json` — paste Gemini key from https://aistudio.google.com/apikey
4. `python main.py`
5. Allow microphone

Key file path: `python/config/api_keys.json` field `gemini_api_key`
